---
name: kr-equity-valuation
description: 한국 상장기업의 주식 밸류에이션을 DART 공시 원문 기반으로 수행한다. 사용자가 특정 종목의 적정주가·목표가·DCF·PER·rNPV·적정PBR을 요청하거나, "이 종목 밸류에이션 해줘", "적정주가 얼마야", "고평가야 저평가야", "매수타점", "기대가 얼마나 반영됐어"라고 묻거나, 한국 종목명·6자리 종목코드와 함께 가치평가를 요청할 때 사용한다. 반도체장비·건설조선·소비재유통·플랫폼게임·제약바이오·금융 6개 산업 유형별 모듈을 갖추고 있으며, 산업 판별 후 해당 방법론을 적용한다. 산출물은 3계층(실현/IR계획/공시계획) 모델 엑셀과 기대가치 분해표다.
---

# 한국 주식 밸류에이션 (공시 원문 기반)

## Overview

증권사 추정치나 언론 기사가 아니라 **DART 정기보고서 원문**을 1차 자료로 삼아
DCF와 PER 밸류에이션을 산출하고, **현재 주가 중 얼마가 검증된 가치이고 얼마가
제3자의 미검증 기대인지**를 분리한다.

이 스킬의 핵심 주장: 밸류에이션의 실패는 계산이 아니라 **가정의 출처를 섞는 데서**
나온다. 실적·회사 목표·애널리스트 추정을 한 모델에 뭉개 넣으면 결과가 그럴듯해 보이지만
반증이 불가능해진다.

## 5대 설계 원칙

| | 원칙 | 내용 |
|---|---|---|
| P1 | **3계층 분리** | ①실현(감사받은 실적) / ②IR계획(회사 공개 목표) / ③공시계획(공시된 상한)을 **절대 섞지 않는다**. **③의 정의는 산업마다 다르다** — `references/industry/` 참조 |
| P2 | **기대 분리** | 현재 EV − 확인가치(①) = 제3자 기대가치. 이 값을 별도 계상하고 **사업 단위(대수·수주액)로 환산**해 달성 가능성을 검증한다 |
| P3 | **순차입금 명시** | DCF는 EV를 낸다. 자기자본가치 = EV − 순차입금. **이 차감 누락이 가장 흔한 치명적 오류다.** 단 금융업은 예외(차입이 영업자산) |
| P4 | **출처 등급** | ①공시원문 ②회사IR ③증권사추정 ④언론추정. **③④는 모델 본체에 넣지 않는다.** 참고로만 병기 |
| P5 | **반증 우선** | 시나리오를 지지하는 근거보다 무너뜨리는 근거를 먼저 쓴다 |

## Workflow

### 0단계 — 산업 유형 판별 【반드시 먼저】

`references/industry/README.md`를 읽고 유형을 확정한다.
**수익인식 방식(한 시점 vs 기간에 걸쳐)을 반기보고서 주석에서 먼저 확인한다.**

| 유형 | 모듈 | ③계층 정의 | DCF |
|---|---|---|---|
| 수주형 장비 | `order-equipment.md` | 수주잔고 전량 | 사이클 정규화 필수 |
| 건설·조선 | `construction-shipbuilding.md` | 수주잔고 × 진행률 | 적합 |
| 소비재·유통 | `consumer-retail.md` | 출점계획 완료 | 적합 |
| 플랫폼·게임 | `platform-game.md` | 신작 파이프라인 | 부적합 |
| 제약·바이오 | `pharma-bio.md` | 파이프라인 상업화 | **rNPV** |
| 금융 | `financials.md` | 자본정책 완료 | **P/B-ROE** |

유형을 확정하기 전에 지표를 계산하지 않는다. **지표 자체가 유형에 종속된다.**

### 1단계 — 공시 원문 수집 (건너뛰지 말 것)

```bash
python scripts/dart.py find  <회사명> <YYYYMMDD>   # 접수번호
python scripts/dart.py facts <접수번호>            # 핵심 7항목 일괄 추출
```

`facts`가 뽑는 7항목: 수주잔고 · 계약부채 · 차입금 · 매출채권연령 · 대손충당금 ·
고객집중도 · 요약재무. 이게 이 스킬의 판단 근거 전부다.

**시세·주식수·베타·컨센서스는 DART에 없다.** `comp.wisereport.co.kr/company/c1010001.aspx?cmp_cd=<종목코드>`를
web_fetch 한다. (네이버·알파스퀘어·토스는 JS 렌더링이라 값이 안 나온다.)

### 2단계 — 검증 지표 산출

**공통 지표**(전 산업): 순차입금·이자보상배율, 회사 가이던스 변경 이력.
**산업별 지표**: 0단계에서 정한 모듈의 1~3순위 지표.

아래는 **수주형 장비산업 기준**이다. 다른 유형이면 해당 모듈을 따른다.

- **계약부채(선수금) 증감** — 수주잔고의 진위를 가리는 단일 최강 지표.
  회사 발표 수주잔고는 취소·연기가 가능하지만 선수금은 현금이 실제로 들어온 것이다.
  `선수금 ÷ 수주잔고 = 현금선수령률`을 반드시 계산한다.
- **매출채권 회전율 3개년** — 방향이 중요하다. 절대값이 아니라 추세.
- **연령분석 + 대손충당금 방향** — 충당금 *환입*이면 대손이 아니다. *전입*이면 대손이다.
  회수일이 길어졌다는 이유만으로 대손이라고 쓰면 틀린다.

### 3단계 — 3계층 정의

| 계층 | 매출 기준 | 근거 |
|---|---|---|
| ① 실현 | 최근 반기 실적 × 2 | 감사받은 숫자. 추가 가정 0 |
| ② IR계획 | 회사가 공개 발표한 연간 목표 | 회사 IR·실적발표 인용 |
| ③ 공시계획 | 반기보고서 수주잔고 전량 인식 | 공시된 상한 |

**회사 가이던스가 기중에 바뀌었는지 반드시 확인한다.** 하향은 강력한 부정 신호다.

### 4단계 — 모델 생성

```bash
python scripts/build_model.py    # valuation.xlsx 생성 (6시트)
python /mnt/skills/public/xlsx/scripts/recalc.py valuation.xlsx
```

`build_model.py` 상단의 `TIER` / `CO` / `ASSUM` 딕셔너리만 종목에 맞게 교체한다.
**recalc가 `errors_found`면 절대 전달하지 않는다.**

생성 시트: `0.설계서` `1.가정` `2·3.DCF` `4.PER` `5.기대가치분해`

### 5단계 — 기대가치 분해 (이 스킬의 차별점)

```
현재 EV          = 시가총액 + 순차입금
확인가치         = ①실현 DCF의 EV
제3자 기대가치   = 현재 EV − 확인가치
잔여 기대        = 현재 EV − ③공시계획 EV     ← 공시로도 설명 안 되는 부분
```

잔여가 **양수**면 시장이 미공시 이벤트를 선반영한 것이다. 이때 반드시
`잔여 ÷ 적용PER ÷ (1−세율) ÷ OPM = 필요 추가매출`로 환산하고, 가능하면
제품 단가로 나눠 **"연 몇 대를 더 팔아야 하는가"**까지 내려간다.

잔여가 **음수**면 시장이 공시된 수주잔고조차 할인하고 있다는 뜻이다.

### 6단계 — 트리거 스펙 작성

`references/trigger_template.md`를 따라 종목별 모니터링 트리거를 만든다.
각 트리거는 **발동 조건 + 계층 이동 판정 기준**을 숫자로 명시한다.

## 절대 하지 말 것

- **순차입금 차감 누락.** EV를 적정주가라고 부르는 순간 모델 전체가 무의미해진다.
- **증권사 추정치를 기준연도로 사용.** ②계층에 회사 발표 대신 애널리스트 추정을 넣으면
  ①과 ②의 구분이 사라진다.
- **결론을 먼저 내고 시나리오 확률을 맞추기.** 확률은 공시 증거에서 역산한다.
- **"믿지 말라"는 단서를 결론 뒤에 붙이기.** 못 믿을 숫자면 처음부터 쓰지 않는다.
- **회수일 증가 = 대손으로 단정.** 충당금 방향과 연령분석을 먼저 본다.
- **미반영 호재 발굴.** 이 스킬은 기대를 *측정*하지 *생성*하지 않는다.
- **산업 판별 없이 6대 지표 적용.** 수주잔고 없는 회사에 계약부채/수주잔고를 계산하려 들면
  모델이 아니라 착오다.
- **사이클 기업의 정점·저점 마진을 영구성장에 투입.** mid-cycle로 정규화하거나
  exit multiple을 쓴다.
- **바이오·금융에 DCF 적용.** 각각 rNPV, P/B-ROE를 쓴다.

## 알려진 함정

- `web_fetch`는 `dart.fss.or.kr` 조회 URL과 이전 검색결과에 없던 URL을 거부한다 → `scripts/dart.py` 사용
- `kind.krx.co.kr` 원문은 403 (봇 차단) → DART 뷰어 경로만 동작
- FnGuide `comp.fnguide.com`은 robots 차단, `comp.wisereport.co.kr`은 허용
- wisereport의 Financial Summary 표는 JS 로딩이라 fetch로 안 잡힌다. 시세·주식수·베타·컨센서스만 취한다
- 밸류라인 재무상태표의 '선수금' 행은 **기타유동부채 내 계약부채와 다른 항목**이다.
  선수금은 반드시 반기보고서 주석에서 확인한다 (이 착오로 판단이 뒤집힌 사례 있음)
- 연결매출을 연도별로 잘못 매칭하기 쉽다. 반기보고서 「매출실적」 표에서 종속회사 합산으로 직접 확인한다
- **같은 수주산업이라도 수익인식이 다르다.** 장비는 시점인식(설치완료), 건설·조선은 진행기준.
  진행기준 기업에 "수주잔고 전량 인식" ③계층을 적용하면 수년치 매출을 1년에 몰아넣는 오류가 된다

## References

- `references/industry/README.md` — **산업 판별 라우터 (0단계 필독)**
- `references/industry/*.md` — 6개 산업 유형별 계층 정의·지표·방법론
- `references/checks.md` — 검증 지표 계산식 (수주형 장비 기준)
- `references/trigger_template.md` — 모니터링 트리거 작성 양식
- `scripts/dart.py` — DART 원문 추출기
- `scripts/build_model.py` — 엑셀 모델 생성기

## 보고 원칙

산출 결과는 투자 권유가 아니라 공시 기반 모델링임을 명시한다.
단일 목표가를 제시하지 않고 **3계층 레인지와 기대가치 분해**로 답한다.
