version: 1
fixture:
  workflow_samples: 40
  freeze_samples: 200
  warmup_runs: 5
captured_baseline:
  source: github_actions_pr_66_run_339
  head_sha: 2f6b2a53ad13ec73753859150d183797fca81576
  runner: ubuntu-24.04_python-3.11.16
  freeze_p95_ms: 0.066
  workflow_p95_ms: 4.345
  workflow_peak_kib: 136.9
  context_copy_calls: 2
budgets:
  max_freeze_p95_ms: 0.5
  max_workflow_p95_ms: 8.0
  max_workflow_peak_kib: 512.0
  max_context_copy_calls: 2
policy:
  - Benchmark the canonical Control Plane stage sequence with the production Unit Contract registry preloaded.
  - Timing and memory budgets retain material runner-noise headroom over the captured baseline without permitting multi-fold regressions.
  - Treat top-level context full-copy count as a deterministic structural budget; stage-local output merges are not copies.
  - A breach must fail CI and identify the observed metric and limit.
