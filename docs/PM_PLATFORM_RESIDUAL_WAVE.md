# PM residual wave — Platform

This branch owns the remaining packaging/runtime-resource acceptance scope. Acceptance requires a clean installed-wheel test outside the repository checkout plus the existing complete CI suite.
